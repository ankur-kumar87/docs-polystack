---
title: "Agent Configuration"
description: "Deploy Monitoring monitoring agents via the deployment console or manually, configure custom scrape targets, and manage per-node agent authentication tokens."
sidebarTitle: "Agent Configuration"
---

import AdminWarning from '/snippets/admin-warning.mdx';

## Overview

Monitoring agents run on every managed node and are responsible for collecting host-level
metrics, forwarding logs, and reporting health status to the Monitoring platform. Agents are
deployed automatically when a node is registered in the deployment console, or manually for nodes
outside the deployment console management.

<AdminWarning />

<Note>
  **Prerequisites**
  - Administrator credentials with the `admin` role
  - Access to **the deployment console** (the **Polystack Dashboard**) for node management
  - SSH access to target nodes for manual agent installation
</Note>

---

## Deploy via the deployment console

<Steps titleSize="h3">
  <Step title="Register the node" icon="plus">
    Navigate to **the deployment console → Infrastructure → Nodes → Add Node**. Provide the node
    IP, hostname, and role. the deployment console installs and configures the Monitoring agent as part
    of the node onboarding process.
  </Step>
  <Step title="Verify agent registration" icon="circle-check">
    Navigate to **Monitor Center > Monitoring** (Agents, admin view). The new node appears
    with status **Active** within 2–3 minutes of registration.

    ```bash title="Check agent status via CLI"
    monitoring agent list --status all
    ```

    <Check>Agent shows `ACTIVE` and last-seen timestamp is within the scrape interval.</Check>
  </Step>
  <Step title="Configure custom scrape targets" icon="settings">
    If the node runs application-specific metric endpoints (e.g., a database exporter),
    add them as additional scrape targets:

    Navigate to **Monitor Center > Monitoring** (Add Scrape Target, admin view) and
    provide the endpoint URL and scrape interval.

    <Tip>
      Metric endpoints must be accessible from the Monitoring collector nodes. Ensure
      security group rules permit inbound connections on the target port from the
      collector IP range.
    </Tip>
  </Step>
</Steps>

---

## Manual Agent Installation

For nodes not managed by the deployment console, install and configure the agent manually.

<Steps titleSize="h3">
  <Step title="Install the agent package" icon="download">
    ```bash title="Install Monitoring agent"
    apt install monitoring-agent
    ```
  </Step>
  <Step title="Retrieve an agent token" icon="key">
    Generate an agent authentication token from the Monitoring portal:
    ```bash title="Create agent token"
    monitoring agent token create \
      --node <HOSTNAME> \
      --expires 365d
    ```

    Store the token securely — it cannot be retrieved after the command completes.
  </Step>
  <Step title="Configure the agent" icon="file-text">
    Edit `/etc/monitoring/agent.yaml`:

    ```yaml title="/etc/monitoring/agent.yaml"
    server:
      endpoint: https://monitoring.polystack.internal:9090
      auth_token: <AGENT_AUTH_TOKEN>

    node:
      hostname: compute-node-04
      labels:
        role: compute
        az: zone-a

    scrape_interval: 30s
    log_paths:
      - /var/log/syslog
      - /var/log/nova/nova-compute.log
    ```
  </Step>
  <Step title="Start and enable the agent" icon="play">
    ```bash title="Enable and start Monitoring agent"
    systemctl enable --now monitoring-agent
    ```

    ```bash title="Verify agent is running"
    systemctl status monitoring-agent
    ```

    <Check>Agent service is `active (running)` and registers with Monitoring within 30 seconds.</Check>
  </Step>
</Steps>

---

## Managing Agent Tokens

<Tabs>
  <Tab title="Token Operations" icon="key">
    ```bash title="List all agent tokens"
    monitoring agent token list
    ```

    ```bash title="Create a new token"
    monitoring agent token create --node compute-node-04 --expires 365d
    ```

    ```bash title="Revoke a token (e.g., for decommissioned nodes)"
    monitoring agent token revoke <TOKEN_ID>
    ```
  </Tab>
  <Tab title="Token Rotation" icon="refresh-cw">
    Rotate agent tokens periodically (recommended: every 365 days) or immediately
    if a node is decommissioned or suspected compromised.

    <Steps titleSize="h3">
      <Step title="Generate a new token" icon="plus">
        ```bash title="Create replacement token"
        monitoring agent token create --node compute-node-04 --expires 365d
        ```
      </Step>
      <Step title="Update agent configuration" icon="settings">
        Update `/etc/monitoring/agent.yaml` on the node with the new token, then restart:
        ```bash title="Restart agent with new token"
        systemctl restart monitoring-agent
        ```
      </Step>
      <Step title="Revoke the old token" icon="trash">
        ```bash title="Revoke old token"
        monitoring agent token revoke <OLD_TOKEN_ID>
        ```

        <Check>Old token is revoked and agent authenticates successfully with the new token.</Check>
      </Step>
    </Steps>
  </Tab>
</Tabs>

---

## Troubleshooting Agents

| Symptom | Diagnostic | Resolution |
|---------|-----------|-----------|
| Agent shows `offline` | `systemctl status monitoring-agent` | Restart service; check token validity |
| Agent not in list | `monitoring agent list --status all` | Verify token; re-register node |
| High scrape latency | `monitoring agent stats --node <HOST>` | Reduce scrape targets; scale up node resources |
| Authentication errors | Check `/var/log/monitoring/agent.log` | Rotate token; verify endpoint URL |

---

## Next Steps

<CardGroup cols={2}>
  <Card title="Metric Endpoints" href="/services/monitoring/admin-guide/metric-endpoints" color="#bf9667">
    Add custom application scrape targets to agents
  </Card>
  <Card title="Log Collection" href="/services/monitoring/admin-guide/log-collection" color="#bf9667">
    Configure which log files are forwarded by each agent
  </Card>
  <Card title="Security" href="/services/monitoring/admin-guide/security" color="#bf9667">
    Agent token management and dashboard access control
  </Card>
  <Card title="Troubleshooting" href="/services/monitoring/admin-guide/troubleshooting" color="#bf9667">
    Diagnose agent connectivity and metric collection issues
  </Card>
</CardGroup>
